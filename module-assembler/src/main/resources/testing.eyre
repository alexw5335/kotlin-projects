import kernel32::WriteFile
import kernel32::ExitProcess



enum TestEnum {
	ENTRY_A,
	ENTRY_B,
	ENTRY_C
}



main:
	sub rsp, 56
	mov ecx, -11
	lea rdx, [main]
	mov r8d, TestEnum.ENTRY_C
	mov r9, [rsp + 40]
	mov qword [rsp + 32], 0
	call WriteFile
	call ExitProcess